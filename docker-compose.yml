version: '3.8'

services:
  analyzer:
    build: .
    volumes:
      - ./output:/app/output
      - ./.env:/app/.env
    environment:
      - OUTPUT_DIR=/app/output
    command: ["--help"]

  # Example with local LLM proxy (optional)
  # llm-proxy:
  #   image: ghcr.io/open-webui/open-webui:main
  #   ports:
  #     - "3000:8080"
  #   volumes:
  #     - ollama-data:/root/.ollama
